---
phase: 01-foundation-and-soundboard
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/components/SoundButton.tsx
  - src/components/SoundGrid.tsx
  - src/components/ListTabBar.tsx
  - src/screens/HomeScreen.tsx
  - app/_layout.tsx
  - src/hooks/useSoundboard.js
  - src/utils/audioBridge.js
autonomous: true
requirements:
  - SND-01
  - SND-02
  - SND-03
  - AUD-04

must_haves:
  truths:
    - "User can see audio items in a 3-column grid with thumbnail or styled title"
    - "Tapping a button plays audio with haptic feedback"
    - "Tapping a different button stops the current sound and plays the new one"
    - "Tapping the currently playing button stops it (toggle)"
    - "Active button is visually highlighted with a glow/pulse animation"
    - "Audio plays from locally cached file in documentDirectory"
    - "Horizontal tab bar shows All Sounds and user-created lists"
  artifacts:
    - path: "src/components/SoundButton.tsx"
      provides: "Grid button with thumbnail, gradient overlay, active highlight, tap/long-press handlers"
      min_lines: 80
    - path: "src/components/SoundGrid.tsx"
      provides: "FlatList wrapper rendering SoundButton items in 3-column grid"
      min_lines: 40
    - path: "src/components/ListTabBar.tsx"
      provides: "Horizontal scrollable tab bar for list switching"
      min_lines: 50
    - path: "src/screens/HomeScreen.tsx"
      provides: "Main screen composing ListTabBar + SoundGrid + header with Edit button"
      min_lines: 80
  key_links:
    - from: "src/components/SoundButton.tsx"
      to: "src/services/AudioPlayerService.ts"
      via: "tap handler calls playSound/stopSound"
      pattern: "import.*AudioPlayerService"
    - from: "src/components/SoundButton.tsx"
      to: "expo-haptics"
      via: "impactAsync on tap"
      pattern: "Haptics\\.impactAsync"
    - from: "src/components/SoundGrid.tsx"
      to: "src/store/soundboardStore.ts"
      via: "reads sounds filtered by activeListId"
      pattern: "useSoundboardStore"
    - from: "src/screens/HomeScreen.tsx"
      to: "src/components/ListTabBar.tsx"
      via: "renders tab bar with lists from store"
      pattern: "ListTabBar"
    - from: "src/components/SoundButton.tsx"
      to: "expo-linear-gradient"
      via: "LinearGradient overlay on thumbnail buttons"
      pattern: "LinearGradient"
---

<objective>
Build the core soundboard UI: SoundButton with thumbnail support and active highlight, SoundGrid with 3-column FlatList, ListTabBar for switching between lists, and HomeScreen composing them together. Wire audio playback via AudioPlayerService with haptic feedback and stop-and-restart behavior.

Purpose: This is the visible, interactive heart of the app. After this plan, users can see sound buttons in a grid, tap to play with haptics, and switch between lists via the tab bar.

Output: SoundButton.tsx, SoundGrid.tsx, ListTabBar.tsx, HomeScreen.tsx (all new TypeScript components replacing the old JS files).
</objective>

<execution_context>
@C:/Users/zetaz/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/zetaz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-and-soundboard/01-CONTEXT.md
@.planning/phases/01-foundation-and-soundboard/01-RESEARCH.md
@.planning/phases/01-foundation-and-soundboard/01-01-SUMMARY.md

@src/components/SoundButton.js
@src/screens/HomeScreen.js
@constants/theme.ts

<interfaces>
<!-- Created by Plan 01 — executor uses these directly -->

From src/store/types.ts:
```typescript
export interface AudioItem {
  id: string;
  title: string;
  uri: string;
  color: string;
  thumbnailUri?: string;
  stickerUri?: string;
  listId: string;
  order: number;
  createdAt: number;
  is_deleted: boolean;
  deleted_at?: number;
}

export interface SoundList {
  id: string;
  name: string;
  color: string;
  order: number;
  createdAt: number;
}
```

From src/store/soundboardStore.ts:
```typescript
export const useSoundboardStore: UseBoundStore<StoreApi<SoundboardState>>;
// Key selectors:
// sounds: AudioItem[]
// lists: SoundList[]
// activeSoundId: string | null
// activeListId: string | null
// isEditMode: boolean
// setActiveSoundId(id: string | null): void
// setActiveListId(id: string | null): void
// setEditMode(on: boolean): void
```

From src/services/AudioPlayerService.ts:
```typescript
export function initAudioSession(): Promise<void>;
export function getPlayer(): AudioPlayer;
export function playSound(uri: string): Promise<void>;
export function stopSound(): Promise<void>;
export function releasePlayer(): void;
```

From src/services/FileCacheService.ts:
```typescript
export function ensureDirectories(): Promise<void>;
export function cacheAudioFile(sourceUri: string, filename: string): Promise<string>;
export function cacheThumbnail(sourceUri: string, id: string, ext?: string): Promise<string>;
export function deleteFile(uri: string): Promise<void>;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create SoundButton and SoundGrid components</name>
  <files>src/components/SoundButton.tsx, src/components/SoundGrid.tsx</files>
  <action>
1. Create `src/components/SoundButton.tsx`:
   - Props: `sound: AudioItem`, `onPress: (sound: AudioItem) => void`, `onLongPress: (sound: AudioItem) => void`, `isPlaying: boolean`, `editMode: boolean`
   - Two visual modes per user decision:
     a. **WITH thumbnail** (`sound.thumbnailUri` exists): `expo-image` Image fills entire square button with `contentFit="cover"`, title overlaid at bottom with `LinearGradient` from transparent to rgba(0,0,0,0.75). Use `LinearGradient` from `expo-linear-gradient`.
     b. **WITHOUT thumbnail**: solid `sound.color` background, centered white title text
   - Active highlight (Claude's discretion — use pulsing glow): When `isPlaying` is true, apply animated border + shadow using `react-native-reanimated` `useAnimatedStyle`. Use green (#25D366) border with pulsing shadow opacity (withRepeat + withSequence + withTiming, 600ms per direction). See RESEARCH.md "Active Button Highlight" code example.
   - Tap handler: call `onPress(sound)` — do NOT call AudioPlayerService directly (HomeScreen orchestrates)
   - Long-press handler: call `onLongPress(sound)` — triggers edit modal (built in Plan 03)
   - In edit mode (`editMode=true`): disable tap and long-press (return early), show a drag handle icon (use Ionicons "reorder-three" from @expo/vector-icons) overlaid at top-right
   - Haptic feedback: call `Haptics.impactAsync(Haptics.ImpactFeedbackStyle.Medium)` inside onPress before calling the callback
   - Sizing: Use `Dimensions.get('window').width` to compute button size = `(windowWidth - padding*2 - gap*2) / 3`, square aspect ratio
   - Dark theme: match existing #121212 background convention
   - StyleSheet at bottom of file per project conventions

2. Create `src/components/SoundGrid.tsx`:
   - Props: none (reads from Zustand store directly)
   - Use `useSoundboardStore` with `useShallow` to select: `sounds`, `activeSoundId`, `activeListId`, `isEditMode`, `setActiveSoundId`
   - Filter sounds: exclude `is_deleted === true`, then filter by `activeListId` (null = show all sounds; string = show sounds where `sound.listId === activeListId`). Sort by `order` field ascending.
   - Render `FlatList` with `numColumns={3}`, `keyExtractor={(item) => item.id}`
   - Each item renders `SoundButton` with `isPlaying={item.id === activeSoundId}`
   - `onPress` handler: implement stop-and-restart logic:
     - If `sound.id === activeSoundId`: call `stopSound()` from AudioPlayerService, then `setActiveSoundId(null)` (toggle off)
     - Else: call `playSound(sound.uri)`, then `setActiveSoundId(sound.id)` (plays new, AudioPlayerService.playSound already replaces the current track)
   - `onLongPress` handler: placeholder for now — will be wired to edit modal in Plan 03. For now, just log to console.
   - Listen for audio completion: after calling `playSound`, set up a listener or check mechanism. Use `getPlayer().addListener('playbackStatusUpdate', (status) => { if (status.isLoaded && status.didJustFinish) setActiveSoundId(null) })` — check actual expo-audio event API. The player emits events; listen for `playToCompletion` or equivalent to clear activeSoundId when sound finishes naturally.
   - Empty state: show centered text "No sounds yet" with subtext "Tap + to add your first sound" in muted gray
   - Wrap in `GestureHandlerRootView` if not already provided by a parent
  </action>
  <verify>
    <automated>npx jest --passWithNoTests && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>
  - SoundButton renders two visual modes: thumbnail with gradient overlay, or solid color with centered title
  - Active button has pulsing green glow animation via Reanimated
  - Edit mode shows drag handle and disables tap/long-press
  - Haptic feedback fires on every tap
  - SoundGrid filters by activeListId, excludes soft-deleted, sorts by order
  - Stop-and-restart: tapping new button stops current and plays new; tapping same button toggles off
  - activeSoundId clears when sound finishes playing
  </done>
</task>

<task type="auto">
  <name>Task 2: Create ListTabBar and HomeScreen, remove legacy JS files</name>
  <files>src/components/ListTabBar.tsx, src/screens/HomeScreen.tsx, app/_layout.tsx, src/hooks/useSoundboard.js, src/utils/audioBridge.js</files>
  <action>
1. Create `src/components/ListTabBar.tsx`:
   - Props: none (reads from Zustand store directly)
   - Use `useSoundboardStore` with `useShallow` to select: `lists`, `activeListId`, `setActiveListId`
   - Render a horizontal `ScrollView` (horizontal=true, showsHorizontalScrollIndicator=false)
   - First item: "All Sounds" tab — always present, colored white/default. Active when `activeListId === null`.
   - Subsequent items: each `SoundList` from store sorted by `order`, tab text = `list.name`, tab background/border colored by `list.color`
   - Last item: "+" button to create a new list (just a placeholder `onPress` for now — will be wired in Plan 03)
   - Active tab indicator: filled background with the list's color; inactive tabs have transparent background with colored border or text
   - Style: fixed height ~44px, items have horizontal padding, rounded pill shape per tab
   - Dark theme: tab bar background #1F1F1F, text white

2. Create `src/screens/HomeScreen.tsx` (TypeScript, replacing HomeScreen.js):
   - Imports: `SoundGrid`, `ListTabBar`, `useSoundboardStore`
   - Layout (top to bottom):
     a. Header: dark background (#1F1F1F), title "YapDeck" on the left, "Edit" toggle button on the right. When tapped, toggles `isEditMode` in store via `setEditMode(!isEditMode)`. When in edit mode, button text changes to "Done".
     b. ListTabBar: rendered below header
     c. SoundGrid: fills remaining space (flex: 1)
     d. AddSoundFab: keep existing FAB component in bottom-right corner (import from existing `src/components/AddSoundFab.js` — will be migrated to TS in Plan 03). Hide FAB when `isEditMode` is true.
   - SafeAreaView wrapper for proper padding on notched devices
   - Background: #121212 (existing dark theme)
   - Do NOT implement share intent handling here yet — that belongs to Phase 3. Remove the share intent useEffect from the old HomeScreen.js logic.

3. Update `app/_layout.tsx`:
   - Remove `SoundboardProvider` import and wrapper (Zustand replaces it)
   - Keep `ShareIntentProvider` (needed for Phase 3 — already configured in app.json)
   - Keep `QueryClientProvider` (added in Plan 01)
   - Update the import path: HomeScreen is now at `src/screens/HomeScreen.tsx`

4. Handle legacy files:
   - Do NOT delete `src/hooks/useSoundboard.js` yet if any other file still imports it. After removing from _layout.tsx, check if anything else imports it. If nothing does, delete it.
   - Do NOT delete `src/utils/audioBridge.js` — the AddSoundFab.js still exists and may reference it. It will be fully replaced in Plan 03.
   - Do NOT delete `src/components/SoundButton.js` (old JS version) — the new `.tsx` file replaces it. Delete the old `.js` if no other file imports it.
   - Do NOT delete `src/screens/HomeScreen.js` (old JS version) — the new `.tsx` file replaces it. Delete the old `.js` since `app/index.tsx` should be updated to import from the new path (but index.tsx imports from `../src/screens/HomeScreen` which will resolve to .tsx first if both exist; safer to delete .js).
  </action>
  <verify>
    <automated>npx jest --passWithNoTests && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>
  - ListTabBar shows "All Sounds" + user lists + "+" button in a horizontal scroll
  - HomeScreen composes header (with Edit/Done toggle), ListTabBar, SoundGrid, and AddSoundFab
  - SoundboardProvider removed from _layout.tsx; Zustand is the sole state source
  - Old HomeScreen.js deleted; new HomeScreen.tsx renders correctly
  - Edit mode toggles via header button, disabling tap/long-press on buttons and showing drag handles
  - App builds without TypeScript errors
  </done>
</task>

</tasks>

<verification>
1. `npx jest --passWithNoTests` — existing tests still pass
2. `npx tsc --noEmit` — no TypeScript errors
3. `npx expo start` — app launches showing grid UI with tab bar (even if no sounds exist yet, empty state shows)
4. Manual: If test sounds exist in AsyncStorage from previous usage, they appear in the grid
</verification>

<success_criteria>
- 3-column grid renders sound buttons with correct visual treatment (thumbnail+gradient or color+title)
- Tap plays audio with haptic feedback; tap different button stops current and plays new
- Tap currently-playing button stops it (toggle)
- Active button has visible green pulsing glow animation
- ListTabBar shows All Sounds tab, user lists, and + button
- Header Edit/Done button toggles edit mode (drag handles visible, taps disabled)
- Empty state message shown when no sounds exist
- No TypeScript errors; all existing tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-soundboard/01-02-SUMMARY.md`
</output>
