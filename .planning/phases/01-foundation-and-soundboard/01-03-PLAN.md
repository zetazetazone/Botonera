---
phase: 01-foundation-and-soundboard
plan: 03
type: execute
wave: 3
depends_on: ["01-01", "01-02"]
files_modified:
  - src/components/AddSoundModal.tsx
  - src/components/EditSoundModal.tsx
  - src/components/CreateListModal.tsx
  - src/components/SoundGrid.tsx
  - src/components/ListTabBar.tsx
  - src/components/AddSoundFab.js
  - src/screens/HomeScreen.tsx
  - src/utils/audioBridge.js
  - src/hooks/useSoundboard.js
autonomous: false
requirements:
  - SND-07
  - AUD-01
  - AUD-02
  - AUD-03
  - AUD-05
  - AUD-06

must_haves:
  truths:
    - "User can import a .opus or .m4a file from device storage"
    - "User can name the imported audio and pick a thumbnail from gallery or camera"
    - "User can attach an optional sticker image separate from the thumbnail"
    - "New sound auto-assigns to the currently active list tab"
    - "User can long-press a button to open edit modal and change name, thumbnail, or sticker"
    - "User can delete a sound from the edit modal (soft-delete)"
    - "User can create a new list with name and theme color"
    - "User can reorder sounds in edit mode via drag-and-drop"
    - "User can move a sound between lists via the edit modal"
  artifacts:
    - path: "src/components/AddSoundModal.tsx"
      provides: "Two-step import flow: file pick then creation form"
      min_lines: 120
    - path: "src/components/EditSoundModal.tsx"
      provides: "Edit modal with name, thumbnail, sticker, color, list, delete"
      min_lines: 100
    - path: "src/components/CreateListModal.tsx"
      provides: "Modal to create new list with name and theme color"
      min_lines: 60
  key_links:
    - from: "src/components/AddSoundModal.tsx"
      to: "expo-document-picker"
      via: "getDocumentAsync for file picking"
      pattern: "DocumentPicker\\.getDocumentAsync"
    - from: "src/components/AddSoundModal.tsx"
      to: "src/services/FileCacheService.ts"
      via: "cacheAudioFile + cacheThumbnail + cacheSticker"
      pattern: "cacheAudioFile|cacheThumbnail|cacheSticker"
    - from: "src/components/AddSoundModal.tsx"
      to: "src/store/soundboardStore.ts"
      via: "addSound action"
      pattern: "addSound"
    - from: "src/components/EditSoundModal.tsx"
      to: "src/store/soundboardStore.ts"
      via: "updateSound and deleteSound actions"
      pattern: "updateSound|deleteSound"
    - from: "src/components/SoundGrid.tsx"
      to: "react-native-draggable-flatlist"
      via: "DraggableFlatList in edit mode"
      pattern: "DraggableFlatList"
    - from: "src/components/AddSoundModal.tsx"
      to: "src/utils/fileUtils.ts"
      via: "isAllowedAudioFile validation"
      pattern: "isAllowedAudioFile"
---

<objective>
Implement the complete audio import flow, CRUD operations (create, edit, delete sounds and lists), and drag-and-drop reordering. This is the final plan that makes the soundboard fully functional for local-first use.

Purpose: After this plan, users can import audio files, name them, attach thumbnails and stickers, organize them into colored lists, reorder via drag-and-drop, and edit or delete any sound. The full Phase 1 user experience is complete.

Output: AddSoundModal.tsx (import flow), EditSoundModal.tsx (edit/delete), CreateListModal.tsx (new list), updated SoundGrid.tsx (drag-and-drop), wired HomeScreen.tsx.
</objective>

<execution_context>
@C:/Users/zetaz/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/zetaz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-and-soundboard/01-CONTEXT.md
@.planning/phases/01-foundation-and-soundboard/01-RESEARCH.md
@.planning/phases/01-foundation-and-soundboard/01-01-SUMMARY.md
@.planning/phases/01-foundation-and-soundboard/01-02-SUMMARY.md

@src/components/EditSoundModal.js
@src/components/AddSoundFab.js

<interfaces>
<!-- Created by Plan 01 and Plan 02 — executor uses these directly -->

From src/store/types.ts:
```typescript
export interface AudioItem {
  id: string;
  title: string;
  uri: string;
  color: string;
  thumbnailUri?: string;
  stickerUri?: string;
  listId: string;
  order: number;
  createdAt: number;
  is_deleted: boolean;
  deleted_at?: number;
}

export interface SoundList {
  id: string;
  name: string;
  color: string;
  order: number;
  createdAt: number;
}
```

From src/store/soundboardStore.ts:
```typescript
// Actions available:
addSound(sound: AudioItem): void
updateSound(id: string, updates: Partial<AudioItem>): void
deleteSound(id: string): void  // soft-delete
addList(list: SoundList): void
updateList(id: string, updates: Partial<SoundList>): void
deleteList(id: string): void
reorderSounds(listId: string, orderedIds: string[]): void
setActiveSoundId(id: string | null): void
setActiveListId(id: string | null): void
setEditMode(on: boolean): void
```

From src/services/FileCacheService.ts:
```typescript
export function cacheAudioFile(sourceUri: string, filename: string): Promise<string>;
export function cacheThumbnail(sourceUri: string, id: string, ext?: string): Promise<string>;
export function cacheSticker(sourceUri: string, id: string, ext?: string): Promise<string>;
export function deleteFile(uri: string): Promise<void>;
```

From src/utils/fileUtils.ts:
```typescript
export function isAllowedAudioFile(filename: string): boolean;
export function generateId(): string;
export const ALLOWED_EXTENSIONS: string[];
```

From src/components/SoundGrid.tsx (Plan 02):
```typescript
// Currently uses FlatList — will be updated to conditionally use DraggableFlatList in edit mode
// onLongPress handler currently logs to console — will be wired to EditSoundModal
```

From src/components/ListTabBar.tsx (Plan 02):
```typescript
// "+" button currently placeholder — will be wired to CreateListModal
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build AddSoundModal (import flow) and CreateListModal</name>
  <files>src/components/AddSoundModal.tsx, src/components/CreateListModal.tsx, src/screens/HomeScreen.tsx, src/components/ListTabBar.tsx</files>
  <action>
1. Create `src/components/AddSoundModal.tsx` — Two-step import flow per user decision:

   **Step 1 — File picker** (triggered when modal opens):
   - Call `DocumentPicker.getDocumentAsync({ type: 'audio/*', copyToCacheDirectory: true })` from `expo-document-picker`
   - `copyToCacheDirectory: true` is critical for Android (returns file:// URI instead of content://)
   - Validate with `isAllowedAudioFile(result.assets[0].name)`. If invalid, show Alert "Unsupported format. Please select .opus, .m4a, .mp3, .aac, or .ogg files." and close modal.
   - If user cancels picker, close modal.
   - On valid pick, cache file immediately: `cacheAudioFile(uri, generateId() + ext)` — store the cached URI for Step 2.

   **Step 2 — Creation form modal** (shown after successful pick):
   - Modal with dark background (#1E1E1E per existing pattern)
   - Fields:
     a. **Name** (TextInput): pre-filled with filename minus extension, max 30 chars
     b. **Thumbnail** (optional): "Choose Image" button. Options: "Gallery" (launchImageLibraryAsync) or "Camera" (launchCameraAsync). Both from `expo-image-picker` with `{ mediaTypes: ['images'], allowsEditing: true, quality: 0.8 }`. Show thumbnail preview if selected.
     c. **Color**: Grid of preset colors (reuse existing color palette from EditSoundModal.js pattern). Default: random color.
     d. **Sticker** (optional, separate from thumbnail): same image picker pattern but labeled "Sticker (for WhatsApp sharing)". Show sticker preview if selected.
   - **Save button**:
     - Generate ID via `generateId()`
     - Cache thumbnail if picked: `cacheThumbnail(thumbnailUri, id)`
     - Cache sticker if picked: `cacheSticker(stickerUri, id)`
     - Create `AudioItem` with all fields. `listId` = current `activeListId` from store (if null, use first list's ID or create a "Default" list). `order` = count of existing sounds in that list.
     - Call `addSound(audioItem)` on store
     - Close modal
   - **Cancel button**: delete the cached audio file (cleanup), close modal
   - Props: `visible: boolean`, `onClose: () => void`

2. Create `src/components/CreateListModal.tsx`:
   - Props: `visible: boolean`, `onClose: () => void`
   - Modal with dark background (#1E1E1E)
   - Fields:
     a. **Name** (TextInput): required, max 20 chars
     b. **Theme Color**: grid of preset colors (same palette as sound color picker)
   - **Create button**: generate ID, create `SoundList` with `order` = lists.length, call `addList()`, optionally `setActiveListId(newList.id)` to switch to new list, close modal
   - **Cancel button**: close modal

3. Update `src/screens/HomeScreen.tsx`:
   - Add state: `showAddModal`, `showCreateListModal`
   - Wire AddSoundFab onPress to: open AddSoundModal (set `showAddModal = true`)
   - Render `<AddSoundModal visible={showAddModal} onClose={() => setShowAddModal(false)} />`
   - Render `<CreateListModal visible={showCreateListModal} onClose={() => setShowCreateListModal(false)} />`
   - Pass `onCreateList={() => setShowCreateListModal(true)}` down to ListTabBar

4. Update `src/components/ListTabBar.tsx`:
   - Accept new prop: `onCreateList: () => void`
   - Wire the "+" button to call `onCreateList()` instead of placeholder
  </action>
  <verify>
    <automated>npx jest --passWithNoTests && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>
  - AddSoundModal implements two-step flow: file picker then creation form
  - File extension validated with isAllowedAudioFile; invalid files show Alert and close
  - Audio file cached to documentDirectory immediately after picking
  - Form collects name, thumbnail (gallery/camera), color, optional sticker
  - New sound auto-assigns to current active list
  - CreateListModal collects name and theme color, creates list in store
  - "+" tab button opens CreateListModal
  - FAB opens AddSoundModal
  </done>
</task>

<task type="auto">
  <name>Task 2: Build EditSoundModal, wire drag-and-drop reordering, clean up legacy files</name>
  <files>src/components/EditSoundModal.tsx, src/components/SoundGrid.tsx, src/screens/HomeScreen.tsx, src/components/AddSoundFab.js, src/utils/audioBridge.js, src/hooks/useSoundboard.js</files>
  <action>
1. Create `src/components/EditSoundModal.tsx` (TypeScript, replacing EditSoundModal.js):
   - Props: `sound: AudioItem | null`, `visible: boolean`, `onClose: () => void`
   - When `sound` is null or `visible` is false, render nothing
   - Modal with dark background (#1E1E1E)
   - Fields (all pre-filled from `sound`):
     a. **Name** (TextInput): current title, max 30 chars
     b. **Thumbnail**: show current thumbnail preview (if exists). "Change" button to pick new from gallery/camera. "Remove" button to clear.
     c. **Color**: same color grid, current color highlighted
     d. **Sticker**: show current sticker preview (if exists). "Change" / "Remove" buttons.
     e. **List**: dropdown or picker showing all lists. Current list pre-selected. Changing this moves the sound to a different list.
   - **Save button**:
     - Build `updates: Partial<AudioItem>` from changed fields
     - If thumbnail changed: cache new via `cacheThumbnail()`, delete old via `deleteFile()`. If removed: `deleteFile(old)`, set `thumbnailUri: undefined`
     - If sticker changed: same pattern with `cacheSticker()` / `deleteFile()`
     - If list changed: update `listId`, set `order` to end of new list
     - Call `updateSound(sound.id, updates)`
     - Close modal
   - **Delete button** (red, bottom of modal):
     - Confirm with Alert: "Delete this sound?"
     - Call `deleteSound(sound.id)` — this is soft-delete (store handles is_deleted + deleted_at)
     - Delete local files: `deleteFile(sound.uri)`, `deleteFile(sound.thumbnailUri)`, `deleteFile(sound.stickerUri)` (if they exist)
     - Close modal
   - **Cancel button**: close modal without saving

2. Update `src/components/SoundGrid.tsx` for drag-and-drop:
   - Import `DraggableFlatList`, `ScaleDecorator`, `RenderItemParams` from `react-native-draggable-flatlist`
   - When `isEditMode` is false: render normal `FlatList` (current behavior)
   - When `isEditMode` is true: render `DraggableFlatList` with:
     - Same data, keyExtractor, filtered + sorted sounds
     - `onDragEnd={({ data }) => reorderSounds(activeListId ?? '__all', data.map(d => d.id))}` — Note: reordering in "All Sounds" tab may need special handling. If `activeListId` is null, reordering applies to all sounds globally which may not make sense. Consider: disable drag in "All Sounds" tab OR reorder within the visible set. Simplest: disable drag when activeListId is null (show a toast "Switch to a specific list to reorder").
     - `renderItem` wraps `SoundButton` in `ScaleDecorator`, passes `drag` as the long-press handler
     - IMPORTANT: DraggableFlatList may NOT support `numColumns` directly. Test first. If it breaks:
       - Fallback option A: Group items into rows of 3 and render each row as a single draggable item
       - Fallback option B: Switch to `react-native-reorderable-list` (install if needed)
       - Fallback option C: Use a single-column list with wider items during edit mode only
       - Document which approach was used in the SUMMARY
   - Wire `onLongPress` (non-edit-mode) to open EditSoundModal: add state in HomeScreen for `editingSound`, pass `onLongPress={(sound) => setEditingSound(sound)}` to SoundGrid

3. Update `src/screens/HomeScreen.tsx`:
   - Add state: `editingSound: AudioItem | null`
   - Pass `onEditSound` callback to SoundGrid so it can trigger edit modal
   - Render `<EditSoundModal sound={editingSound} visible={!!editingSound} onClose={() => setEditingSound(null)} />`

4. Clean up legacy files:
   - Delete `src/components/SoundButton.js` (replaced by SoundButton.tsx in Plan 02)
   - Delete `src/components/EditSoundModal.js` (replaced by EditSoundModal.tsx)
   - Delete `src/screens/HomeScreen.js` (replaced by HomeScreen.tsx in Plan 02)
   - Delete `src/hooks/useSoundboard.js` (replaced by Zustand store)
   - Delete `src/utils/audioBridge.js` (replaced by AudioPlayerService + FileCacheService)
   - Update any remaining imports that point to deleted files. Check `app/_layout.tsx` and `app/index.tsx`.
   - Convert `src/components/AddSoundFab.js` to `src/components/AddSoundFab.tsx` — keep existing FAB behavior, just add TypeScript props: `onPress: () => void`, `visible: boolean`. Hide when `visible` is false (edit mode).
  </action>
  <verify>
    <automated>npx jest --passWithNoTests && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>
  - EditSoundModal opens on long-press, shows all editable fields (name, thumbnail, sticker, color, list)
  - Save applies changes to store and updates cached files
  - Delete soft-deletes from store and removes local files
  - Drag-and-drop reordering works in edit mode (or documented fallback approach)
  - All legacy .js files deleted; codebase is pure TypeScript
  - No broken imports; app builds clean
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify complete Phase 1 soundboard experience</name>
  <files>n/a</files>
  <action>
  Human verifies the complete Phase 1 soundboard experience on device/emulator. All automated work is complete in Tasks 1 and 2. This checkpoint confirms the full user flow works end-to-end.

  What was built: Complete Phase 1 soundboard — local-first audio import, grid playback with haptics, lists with theme colors, drag-and-drop reordering, edit/delete flow.
  </action>
  <verify>
  Manual verification steps:
  1. Start the app: `npx expo start` and open on device/emulator
  2. **Import a sound**: Tap the + FAB. File picker opens. Select a .opus or .m4a file. Creation form appears with pre-filled name. Set a name, pick a thumbnail from gallery, pick a color. Save. Sound appears in grid.
  3. **Play the sound**: Tap the button. Audio plays with haptic feedback. Button shows green pulsing glow.
  4. **Stop-and-restart**: Import a second sound. Tap sound 1 (plays). Tap sound 2 (sound 1 stops, sound 2 plays). Tap sound 2 again (stops).
  5. **Create a list**: Tap "+" in the tab bar. Enter name, pick color. List appears as a new tab.
  6. **Switch lists**: Tap the new list tab. Grid shows only sounds in that list (empty if none assigned). Tap "All Sounds" to see all.
  7. **Edit a sound**: Long-press a button. Edit modal opens with current values. Change the name and thumbnail. Save. Changes persist.
  8. **Move between lists**: Long-press a button. In edit modal, change the list assignment. Save. Sound moves to new list.
  9. **Delete a sound**: Long-press a button. Tap Delete. Confirm. Sound disappears from grid.
  10. **Reorder**: Tap "Edit" in header. Drag handles appear. Drag a button to a new position. Tap "Done". Order persists.
  11. **Offline persistence**: Kill and restart the app. All sounds, lists, and order are preserved.
  12. **Thumbnail rendering**: Verify buttons with thumbnails show image + gradient overlay + title at bottom. Buttons without thumbnails show solid color + centered title.
  </verify>
  <done>User confirms "approved" — all 12 verification steps pass on device/emulator</done>
</task>

</tasks>

<verification>
1. `npx jest` — all test suites pass
2. `npx tsc --noEmit` — no TypeScript errors
3. No `.js` files remain in `src/` (all migrated to TypeScript)
4. `grep -r "expo-av" src/` — no references to deprecated library
5. Full manual flow test as described in checkpoint
</verification>

<success_criteria>
- User can import .opus/.m4a files via document picker with extension validation
- Creation form collects name, thumbnail (gallery/camera), color, optional sticker
- Sounds display in 3-column grid with correct visual treatment
- Tap plays with haptics; stop-and-restart works; toggle works
- Long-press opens edit modal; changes persist
- Delete soft-deletes and removes local files
- Lists with theme colors can be created and switched between
- Drag-and-drop reordering works in edit mode
- All data persists across app restarts (Zustand + AsyncStorage)
- Codebase is fully TypeScript (no .js files in src/)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-soundboard/01-03-SUMMARY.md`
</output>
